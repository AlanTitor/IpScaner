spring:
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:postgresql://db:5432/scan
    username: postgres
    password: 1515

ping:
  timeout: 500

cors:
  frontUrl: http://localhost:3000,http://127.0.0.1:3000